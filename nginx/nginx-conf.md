```nginx
http {
    server {
        listen 8080;
        root /Users/vivabm/Web/1_Public/root/index.html; 
    }
}
```

## Command

```bash
    nginx -t # check error
    nginx -s reload # reload nginx server
```
