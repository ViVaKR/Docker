# Ubuntu

## Docker

## Oh-My-Posh Install

```bash
    curl -s https://ohmyposh.dev/install.sh | sudo bash -s
    /user/local/bin
    oh-my-posh get shell
    eval "$(oh-my-posh init bash)"
    exec bash
    . ~/.profile
```
